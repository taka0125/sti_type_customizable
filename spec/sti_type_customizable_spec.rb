require 'spec_helper'

describe StiTypeCustomizable do
  it 'has a version number' do
    expect(StiTypeCustomizable::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
